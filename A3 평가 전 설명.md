# A3 평가 전 설명

안녕하세요. 제가 개발한 프로젝트는 ‘AI 여행 추천 서비스’입니다.

이 서비스는 사용자가 동행 유형, 인원 수, 예산, 여행 스타일, 여행 기간을 입력하면 조건에 어울리는 국내 여행지를 AI가 추천해주는 웹서비스입니다.

화면은 서비스 소개, 여행 추천 받기, 이용 안내의 3개 영역으로 구성하였으며, 메뉴 버튼을 누르면 각 영역으로 이동할 수 있도록 구현하였습니다. **HTML, CSS, JavaScript**로 화면을 만들었고, **모바일**에서도 편리하게 사용할 수 있도록 **반응형 디자인**을 적용하였습니다.

사용자가 여행 조건을 입력하고 추천 버튼을 누르면 JavaScript의 fetch 기능이 입력값을 Python 백엔드로 전달합니다. Python 백엔드는 Vercel Serverless Function으로 구성하였으며, 코디세이에서 제공한 OpenAI 호환 API와 gpt-5-mini 모델을 호출합니다. AI가 생성한 추천 여행지, 추천 이유, 추천 일정과 여행 팁은 다시 웹 화면에 표시됩니다.

개발 과정에서 처음에는 미리 작성한 조건문으로 여행지를 추천했기 때문에 실제 AI 기능이라고 보기 어려운 문제가 있었습니다. 이를 보완하기 위해 실제 AI API를 연결하였습니다. 또한 코디세이 API는 OpenAI 공식 접속 주소가 아니라 별도의 Base URL을 사용한다는 점을 확인하여 코드를 수정하였습니다.

API 키는 코드나 GitHub에 넣지 않고 Vercel의 환경 변수로 관리하여 보안 기준도 적용하였습니다. 필수 입력값 누락, 잘못된 인원 수, API 연결 오류와 응답 지연 상황에 대한 안내 메시지도 구현하였습니다.

마지막으로 GitHub에 전체 소스 코드와 README, 서비스 기획서, 데스크톱 및 모바일 동작 화면, AI 코딩 도구 사용 증빙을 정리하였습니다. Vercel 배포 주소에서 동료가 직접 접속하여 모든 기능을 테스트할 수 있습니다.

이번 프로젝트를 통해 사용자 입력이 JavaScript 요청으로 변환되고, Python 백엔드와 AI API를 거쳐 다시 화면에 표시되는 전체 흐름을 이해할 수 있었습니다. 특히 AI가 만든 코드를 그대로 사용하는 것이 아니라, 오류의 원인을 확인하고 수정하는 과정이 중요하다는 점을 배웠습니다.

<aside>
💡

## 동료 평가 예상 Q&A

---

## Q1. 이 서비스의 핵심 기능은 무엇인가요?

사용자가 동행 유형, 인원 수, 예산, 여행 스타일, 여행 기간을 입력하면 AI가 조건을 분석하여 국내 여행지와 추천 이유, 일정, 여행 팁을 생성하는 기능입니다.

### Q2. 기존 여행 추천 서비스와 어떤 차이가 있나요?

미리 저장된 여행지만 단순히 보여주는 것이 아니라, 사용자가 입력한 여러 조건을 AI가 함께 고려하여 매번 맞춤형 결과를 생성한다는 점이 다릅니다.

### Q3. 사용자가 입력한 정보는 어떻게 AI로 전달되나요?

사용자가 추천 버튼을 누르면 JavaScript의 `fetch()`가 입력값을 JSON 형식으로 Python 백엔드의 `/api/generate` 주소에 전달합니다. 백엔드는 이 내용을 프롬프트로 구성하여 AI API에 요청합니다.

### Q4. 프론트엔드와 백엔드는 각각 무슨 역할을 하나요?

프론트엔드는 사용자가 보고 조작하는 화면을 담당합니다. HTML은 구조, CSS는 디자인과 반응형 화면, JavaScript는 입력 확인과 서버 요청을 담당합니다. Python 백엔드는 API 키를 안전하게 관리하고 AI를 호출한 뒤 결과를 반환합니다.

### Q5. Vercel Serverless Function이 무엇인가요?

별도의 서버를 계속 운영하지 않아도 요청이 들어올 때 Python 함수가 실행되는 방식입니다. 이 프로젝트에서는 `api/generate.py`가 사용자의 요청을 받아 AI API를 호출합니다.

### Q6. 실제 AI를 사용했다는 것은 어떻게 확인할 수 있나요?

처음에는 조건문으로 정해진 여행지를 출력했지만, 최종 버전에서는 코디세이 OpenAI 호환 API의 `gpt-5-mini` 모델을 직접 호출하도록 변경하였습니다. 같은 조건이라도 AI가 추천 이유와 일정, 여행 팁을 새롭게 생성합니다.

### Q7. 개발하면서 가장 어려웠던 점은 무엇인가요?

API 키는 정상적으로 발급되어 있었지만 처음에는 OpenAI 공식 서버 주소로 요청하여 연결 오류가 발생했습니다. 코디세이 문서를 확인한 결과 별도의 Base URL과 지원 모델을 사용해야 한다는 것을 발견했고, 접속 주소와 모델명을 수정하여 해결하였습니다.

### Q8. API 키는 어떻게 보호했나요?

API 키를 HTML, JavaScript, Python 코드나 GitHub에 직접 입력하지 않았습니다. Vercel의 `OPENAI_API_KEY` 환경 변수에 저장하고 Python 백엔드에서만 불러오도록 구성하였습니다.

### Q9. 오류가 발생하면 어떻게 처리되나요?

필수 입력값이 비어 있거나 인원 수가 잘못된 경우 사용자에게 안내 메시지를 표시합니다. API 연결 오류, API 키 미설정, 응답 지연과 예상하지 못한 오류에 대해서도 각각 안내 메시지가 나타나도록 처리하였습니다.

### Q10. 모바일에서도 사용할 수 있나요?

CSS 미디어 쿼리를 적용하여 화면이 작아지면 메뉴가 가로에서 세로로 변경되도록 구성하였습니다. 크롬 개발자 도구의 모바일 화면에서도 입력과 AI 결과 출력을 직접 확인하였습니다.

### Q11. AI가 추천한 정보는 항상 정확한가요?

AI가 생성한 결과이므로 장소의 운영시간이나 요금처럼 변경될 수 있는 정보는 추가 확인이 필요합니다. 현재 서비스는 여행 계획의 초안을 빠르게 만드는 용도이며, 향후 지도나 관광 데이터 API를 연결하면 정확성을 더 높일 수 있습니다.

### Q12. AI 코딩 도구가 만든 코드를 그대로 사용한 건가요?

AI 코딩 도구로 초기 코드와 수정 방향에 도움을 받았지만 그대로 제출하지는 않았습니다. 실제 실행 과정에서 입력값과 백엔드 구조를 확인하고, API 연결 오류의 원인을 찾아 Base URL과 모델명을 직접 수정한 후 재배포와 동작 검증까지 진행하였습니다.

### Q13. GitHub와 Vercel은 각각 왜 사용했나요?

GitHub는 소스 코드와 수정 이력을 저장하기 위해 사용하였고, Vercel은 GitHub의 코드를 실제 웹서비스로 배포하기 위해 사용하였습니다. GitHub의 `main` 브랜치에 코드를 올리면 Vercel이 변경 내용을 감지하여 자동으로 다시 배포합니다.

### Q14. 현재 서비스의 한계는 무엇인가요?

출발 지역을 입력하지 않기 때문에 AI가 이동 시간을 일반적으로 추정할 수 있다는 한계가 있습니다. 또한 실시간 날씨, 교통, 숙박 가격과 관광지 운영 여부는 반영하지 않습니다.

### Q15. 앞으로 어떤 기능을 추가하고 싶나요?

출발 지역, 이동수단, 연령대와 선호 지역을 입력받는 기능을 추가하고 싶습니다. 지도·날씨·관광 데이터 API를 연결하여 실제 이동 거리, 날씨와 운영 정보를 반영하고, 추천 결과를 PDF 여행계획서로 저장하는 기능까지 확장할 수 있습니다.

### Q16. 이번 과제를 통해 무엇을 배웠나요?

HTML·CSS·JavaScript로 만든 화면에서 사용자 입력이 `fetch()` 요청으로 바뀌고, Python 백엔드와 AI API를 거쳐 다시 화면에 표시되는 전체 흐름을 이해하였습니다. 특히 오류 메시지를 보고 문제의 원인을 확인하고 수정하는 과정이 실제 개발에서 중요하다는 것을 배웠습니다.

## 답이 막힐 때 사용할 문장

“그 부분은 현재 버전에서는 구현하지 않았지만, 다음 버전에서 보완할 기능으로 정리해 두었습니다.”

“현재는 여행 계획의 초안을 제공하는 단계이며, 실시간 데이터 API를 연결하면 정확성을 더 높일 수 있습니다.”

“처음에는 오류가 발생했지만, 문서와 실행 결과를 비교하여 원인을 찾고 수정한 뒤 다시 배포하였습니다.”

</aside>