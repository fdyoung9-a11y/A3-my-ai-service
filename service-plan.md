# 서비스 기획서 - A3-my-ai-service

## 1. 서비스 개요

### 서비스명
A3-my-ai-service

### 한 줄 소개
사용자가 여행지를 입력하면 AI가 간단한 여행 추천 문장을 생성해주는 웹서비스입니다.

### 서비스 목적
여행지를 고민하는 사용자가 간단한 입력만으로 여행 추천 정보를 빠르게 확인할 수 있도록 돕는 것이 목적입니다.

---

## 2. 타깃 사용자

- 여행 계획을 간단히 세우고 싶은 사용자
- 특정 지역 여행 아이디어가 필요한 사용자
- AI 기반 추천 서비스를 체험해보고 싶은 사용자

---

## 3. 페이지 / 블록 구성

이 서비스는 한 페이지 안에서 여러 콘텐츠 블록을 전환하는 방식으로 구성되어 있습니다.

### 블록 1. 소개
- 서비스 소개 문구 제공
- 웹서비스의 목적과 사용 방법 간단 안내

### 블록 2. 여행 추천
- 사용자가 여행지를 입력
- 추천 버튼 클릭 또는 Enter 입력으로 AI 추천 실행
- 결과 문장 출력

### 블록 3. 이용 안내
- 서비스 사용 방법 설명
- 입력 예시 및 주의사항 안내

---

## 4. 핵심 기능

### 1) 여행지 입력 기능
사용자가 여행지를 텍스트로 입력할 수 있습니다.

### 2) AI 추천 결과 생성 기능
입력된 여행지를 바탕으로 추천 문장을 생성하여 화면에 표시합니다.

### 3) 블록형 UI 전환 기능
메뉴 또는 버튼을 클릭하면 페이지 이동 없이 해당 블록만 화면에 표시됩니다.

### 4) 반응형 웹 기능
PC와 모바일 환경에서 모두 사용할 수 있도록 반응형 레이아웃을 적용했습니다.

---

## 5. AI 기능 설명

### 입력값
- 여행지 이름
- 예: 서울, 부산, 제주도, 강릉

### 처리 방식
- 프론트엔드에서 사용자의 입력값을 받습니다.
- JavaScript의 `fetch()`를 사용해 `/api/generate`로 요청을 보냅니다.
- 백엔드(Python)가 입력값을 받아 추천 문장을 생성합니다.
- 생성된 결과를 JSON 형태로 반환합니다.
- 프론트엔드가 응답을 받아 화면에 출력합니다.

### 출력값
- 여행 추천 문장
- 예: `제주도 여행을 추천합니다! 대표 명소, 맛집, 휴식 코스를 함께 즐겨보세요.`

---

## 6. 실패 처리 기준

### 1) 빈 입력
- 사용자가 입력 없이 추천을 요청하면 안내 메시지를 표시합니다.
- 예: `여행지를 입력해주세요.`

### 2) API 요청 실패
- 서버 호출 중 문제가 발생하면 오류 안내 메시지를 표시합니다.
- 예: `추천 생성 중 오류가 발생했습니다. 다시 시도해주세요.`

### 3) 응답 지연
- 필요 시 로딩 메시지를 표시할 수 있도록 고려했습니다.
- 예: `추천을 생성하는 중입니다...`

---

## 7. 기술 스택

### 프론트엔드
- HTML
- CSS
- JavaScript

### 백엔드
- Python
- Vercel Serverless Functions

### 배포
- GitHub
- Vercel

---

## 8. 파일 구조

```bash
A3-my-ai-service/
├─ index.html
├─ style.css
├─ app.js
├─ requirements.txt
└─ api/
   └─ generate.py