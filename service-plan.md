# 서비스 기획서 - A3-my-ai-service

## 1. 서비스명
A3-my-ai-service

---

## 2. 서비스 한 줄 소개
사용자가 여행지를 입력하면 AI가 간단한 여행 추천 문장을 생성해주는 반응형 웹서비스입니다.

---

## 3. 서비스 기획 배경

여행을 계획할 때 어디로 갈지 고민하는 경우가 많습니다.  
이 서비스는 사용자가 여행지 이름만 간단히 입력해도 추천 문장을 빠르게 확인할 수 있도록 만들어졌습니다.

또한 본 프로젝트는 단순한 정적 웹페이지를 넘어서,  
프론트엔드와 백엔드를 연결하고 API를 호출하는 전체 흐름을 직접 구현하는 것을 목표로 했습니다.

---

## 4. 서비스 목표

- 사용자가 간단한 입력만으로 여행 추천 결과를 받을 수 있도록 한다.
- HTML, CSS, JavaScript를 활용해 직관적인 사용자 화면을 구현한다.
- Python API와 프론트엔드를 연결해 AI 기능이 포함된 웹서비스 구조를 완성한다.
- PC와 모바일 환경 모두에서 사용할 수 있는 반응형 UI를 제공한다.

---

## 5. 타깃 사용자

- 여행 아이디어가 필요한 사용자
- 간단한 추천 서비스를 빠르게 이용하고 싶은 사용자
- AI 기반 웹서비스 예시를 체험해보고 싶은 사용자
- 프론트엔드와 백엔드가 연결된 학습형 프로젝트 결과물을 보고 싶은 평가자

---

## 6. 서비스 주요 기능

### 6-1. 여행지 입력 기능
사용자가 원하는 여행지를 입력창에 작성할 수 있습니다.

예시:
- 서울
- 부산
- 제주도
- 강릉

### 6-2. AI 여행 추천 생성 기능
입력한 여행지를 바탕으로 추천 문장을 생성하여 결과 영역에 보여줍니다.

출력 예시:
- `제주도 여행을 추천합니다! 대표 명소, 맛집, 휴식 코스를 함께 즐겨보세요.`

### 6-3. 블록형 UI 전환 기능
한 페이지 안에서 여러 콘텐츠 영역을 블록 형태로 나누고,  
사용자가 버튼을 클릭하면 원하는 블록만 보이도록 구성했습니다.

### 6-4. 반응형 웹 기능
화면 크기에 따라 레이아웃이 자동으로 조정되도록 CSS를 구성했습니다.  
특히 모바일 환경에서도 요소가 겹치지 않고 자연스럽게 보이도록 설계했습니다.

### 6-5. 예외 처리 기능
입력값이 비어 있거나 API 호출 중 오류가 발생했을 때 사용자에게 안내 메시지를 제공합니다.

---

## 7. 화면 구성

본 서비스는 단일 페이지 안에서 여러 블록이 전환되는 구조로 구성되어 있습니다.

### 블록 1. 서비스 소개 영역
- 서비스 이름과 소개 문구를 보여줍니다.
- 사용자가 어떤 기능을 사용할 수 있는지 간단히 안내합니다.

### 블록 2. 여행 추천 영역
- 사용자가 여행지를 입력합니다.
- 추천 버튼 클릭 또는 Enter 입력으로 추천을 요청합니다.
- API 응답 결과가 화면에 표시됩니다.

### 블록 3. 이용 안내 영역
- 서비스 사용 방법을 설명합니다.
- 입력 예시와 결과 확인 방식을 안내합니다.

---

## 8. 사용자 이용 흐름

1. 사용자가 웹페이지에 접속합니다.
2. 화면에서 여행 추천 블록을 확인합니다.
3. 입력창에 여행지를 작성합니다.
4. 추천 버튼을 클릭하거나 Enter 키를 누릅니다.
5. 프론트엔드가 `/api/generate` 경로로 요청을 보냅니다.
6. 백엔드가 입력값을 바탕으로 추천 문장을 생성합니다.
7. 결과가 JSON 형태로 반환됩니다.
8. 프론트엔드가 응답을 받아 화면에 추천 결과를 출력합니다.

---

## 9. AI 기능 설계

### 입력값
- 사용자가 입력한 여행지 문자열

예시:
```json
{
  "destination": "부산"
}