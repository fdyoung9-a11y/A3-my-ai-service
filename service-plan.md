# AI 여행 추천 서비스 기획서

## 1. 서비스명

AI 여행 추천 서비스

## 2. 서비스 한 줄 소개

사용자의 여행 조건을 바탕으로 AI가 맞춤형 국내 여행지, 추천 이유, 일정과 여행 팁을 생성해주는 반응형 웹서비스입니다.

## 3. 기획 배경

여행을 계획할 때 동행자, 예산, 여행 기간과 취향을 모두 고려하여 여행지를 정하는 일은 생각보다 어렵습니다.

본 서비스는 사용자가 간단한 여행 조건을 선택하면 AI가 해당 조건을 분석하여 적합한 국내 여행지를 추천하도록 기획했습니다.

또한 HTML·CSS·JavaScript로 구현한 프론트엔드와 Python 백엔드, AI API를 연결하여 실제 배포 가능한 웹서비스를 완성하는 것을 목표로 합니다.

## 4. 서비스 목적

- 사용자의 조건에 맞는 국내 여행지를 빠르게 추천합니다.
- 여행지뿐 아니라 추천 이유, 일정과 실용적인 팁을 함께 제공합니다.
- 복잡한 검색 없이 간단한 입력만으로 여행 아이디어를 얻도록 돕습니다.
- 프론트엔드와 Python 백엔드가 연결된 AI 웹서비스를 구현합니다.

## 5. 타깃 사용자

- 국내 여행지를 결정하지 못한 사용자
- 짧은 시간 안에 여행 계획의 초안을 만들고 싶은 사용자
- 동행자와 예산에 적합한 여행 아이디어가 필요한 사용자
- AI 기반 웹서비스를 체험하고 싶은 사용자

## 6. 화면 구성

서비스는 하나의 웹페이지 안에 다음 3개 섹션으로 구성합니다.

### 6.1 서비스 소개

- 서비스의 목적과 주요 기능을 안내합니다.
- 사용자가 입력할 수 있는 여행 조건을 보여줍니다.

### 6.2 여행 추천 받기

사용자가 다음 조건을 입력합니다.

- 동행 유형
- 인원 수
- 예산
- 여행 스타일
- 여행 기간

입력 완료 후 `여행 추천 받기` 버튼을 누르면 AI 추천 결과를 표시합니다.

### 6.3 이용 안내

- 여행 추천 서비스를 사용하는 순서를 안내합니다.
- 입력 예시와 결과 확인 방법을 제공합니다.

## 7. 핵심 기능

### 7.1 메뉴 이동

상단의 메뉴 버튼을 누르면 선택한 섹션만 화면에 표시합니다.

### 7.2 여행 조건 입력

선택 메뉴와 숫자 입력창을 사용하여 사용자의 여행 조건을 받습니다.

### 7.3 AI 여행 추천

프론트엔드가 입력 정보를 `/api/generate`로 전송하면 Python 백엔드가 코디세이 OpenAI 호환 API를 호출합니다.

AI는 입력 조건을 분석하여 새로운 추천 내용을 생성합니다.

### 7.4 결과 출력

AI가 생성한 다음 내용을 결과 영역에 표시합니다.

- 추천 여행지
- 추천 이유
- 추천 일정
- 여행 팁

### 7.5 반응형 화면

데스크톱과 모바일 환경에서 메뉴, 입력창과 결과 영역이 화면 크기에 맞게 표시되도록 구성합니다.

## 8. 사용자 이용 흐름

1. 사용자가 배포된 웹서비스에 접속합니다.
2. `여행 추천 받기` 메뉴를 선택합니다.
3. 동행 유형을 선택합니다.
4. 인원 수를 입력합니다.
5. 예산을 선택합니다.
6. 여행 스타일을 선택합니다.
7. 여행 기간을 선택합니다.
8. `여행 추천 받기` 버튼을 누릅니다.
9. JavaScript가 입력값을 검증합니다.
10. 프론트엔드가 Python API에 요청을 보냅니다.
11. Python 백엔드가 AI API를 호출합니다.
12. 생성된 추천 결과가 웹 화면에 표시됩니다.

## 9. AI 기능 설계

### 9.1 입력값

```json
{
  "companion": "연인",
  "people": "2",
  "budget": "중간",
  "style": "힐링",
  "days": "1박2일"
}
```

### 9.2 처리 과정

1. Python 백엔드가 JSON 입력값을 받습니다.
2. 필수 입력값과 인원 수를 검증합니다.
3. 입력값을 여행 추천 프롬프트로 구성합니다.
4. 환경 변수에서 API 키를 불러옵니다.
5. 코디세이 OpenAI 호환 API의 `gpt-5-mini` 모델을 호출합니다.
6. AI 응답을 JSON 형식으로 프론트엔드에 반환합니다.

### 9.3 출력 형식

```text
[추천 여행지]
조건에 적합한 국내 여행지

[추천 이유]
해당 여행지를 추천한 구체적인 이유

[추천 일정]
여행 기간에 맞는 핵심 일정

[여행 팁]
예산과 이동에 도움이 되는 실용적인 정보
```

### 9.4 사용자 제공 가치

- 여행지 탐색 시간 단축
- 여행 조건을 반영한 맞춤형 결과
- 여행 일정 초안 제공
- 예산과 일정에 도움이 되는 실용적인 팁 제공

## 10. 실패 처리 기준

### 10.1 빈 입력

필수 항목이 누락되면 다음 메시지를 표시합니다.

```text
모든 항목을 입력해 주세요.
```

### 10.2 잘못된 인원 수

인원 수가 1명 미만이거나 숫자가 아니면 안내 메시지를 표시합니다.

### 10.3 API 키 미설정

환경 변수에 API 키가 없으면 AI 서비스 설정이 완료되지 않았다는 메시지를 표시합니다.

### 10.4 API 연결 오류

AI API 호출이 실패하면 연결 오류 안내 메시지를 표시합니다.

### 10.5 응답 지연

정해진 시간 안에 응답하지 않으면 잠시 후 다시 시도하도록 안내합니다.

## 11. 기술 구성

### 프론트엔드

- HTML
- CSS
- JavaScript
- Fetch API

### 백엔드

- Python
- Flask
- Vercel Serverless Functions

### AI

- 코디세이 OpenAI 호환 API
- `gpt-5-mini`

### 저장 및 배포

- GitHub
- Vercel

## 12. 보안 계획

- API 키는 Vercel의 `OPENAI_API_KEY` 환경 변수로 관리합니다.
- API 키를 HTML이나 JavaScript에 포함하지 않습니다.
- API 키를 GitHub에 업로드하지 않습니다.
- 모든 AI 요청은 Python 백엔드를 통해 처리합니다.
- 키가 노출되면 즉시 기존 키를 폐기하고 새 키를 발급합니다.

## 13. 검증 계획

- 3개 메뉴의 정상 이동 확인
- 모든 입력 항목의 정상 작동 확인
- 빈 입력값 안내 확인
- 잘못된 인원 수 처리 확인
- AI 추천 결과 출력 확인
- API 오류 메시지 확인
- 데스크톱 화면 확인
- 모바일 화면 확인
- Vercel 배포 URL에서 전체 기능 확인
- GitHub에 API 키가 노출되지 않았는지 확인

## 14. 기대 효과

사용자는 복잡한 검색 과정을 줄이고 여행 조건에 맞는 여행지와 일정 초안을 빠르게 얻을 수 있습니다.

학습자는 프론트엔드 입력이 JavaScript의 요청으로 변환되고, Python 백엔드와 AI API를 거쳐 결과가 화면에 표시되는 전체 흐름을 이해할 수 있습니다.